Temporary note: investigate delayed OTC settlement. Current performance resolver can only settle against in-memory closed candles; cache tail(500) is ~8h20m at 1m. A pending signal older than that can never resolve after a missed settlement cycle. Proposed fix: increase OTC local cache retention to >=2000 candles (~33h) and add resolver logging for missing candles/errors; do not alter signal generation logic.